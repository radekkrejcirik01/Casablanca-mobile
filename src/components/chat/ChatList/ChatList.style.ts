import { StyleSheet } from 'react-native';

export const ChatListStyle = StyleSheet.create({
    flatList: {
        paddingBottom: '100%'
    },
    container: {
        marginTop: 20
    },
    title: {
        fontSize: 15,
        color: 'white',
        fontWeight: 'bold'
    }
});
