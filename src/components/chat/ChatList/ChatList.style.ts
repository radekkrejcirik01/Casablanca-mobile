import { StyleSheet } from 'react-native';

export const ChatListStyle = StyleSheet.create({
    contentContainer: {
        paddingTop: 40,
        paddingHorizontal: 12,
        paddingBottom: 20
    }
});
