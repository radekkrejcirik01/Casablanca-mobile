import { StyleSheet } from 'react-native';
import COLORS from '@constants/COLORS';

export const InfoContentStyle = StyleSheet.create({
    scrollView: {
        padding: 20
    },
    text: {
        color: COLORS.WHITE,
        fontWeight: '500'
    }
});
