import { StyleSheet } from 'react-native';

export const PlaceTagsStyle = StyleSheet.create({
    container: {
        justifyContent: 'center',
        flexDirection: 'row',
        flexWrap: 'wrap'
    }
});
