import { StyleSheet } from 'react-native';
import COLORS from '@constants/COLORS';

export const SaveHeaderStyle = StyleSheet.create({
    container: {
        paddingRight: 20
    },
    text: {
        color: COLORS.WHITE,
        fontWeight: 'bold'
    }
});
