import { StyleSheet } from 'react-native';

export const MatchListStyle = StyleSheet.create({
    flatList: {
        marginLeft: -15, // ChatScreen marginHorizontal is 15
        paddingLeft: 15,
        marginTop: 20,
        marginBottom: 25
    }
});
