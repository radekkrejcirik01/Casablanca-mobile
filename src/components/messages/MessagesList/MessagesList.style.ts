import { StyleSheet } from 'react-native';

export const MessagesListStyle = StyleSheet.create({
    container: {
        marginTop: 20
    },
    title: {
        fontSize: 15,
        color: 'white',
        fontWeight: 'bold'
    }
});
