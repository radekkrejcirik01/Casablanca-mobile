import { StyleSheet } from 'react-native';

export const PhotoHorizontalListStyle = StyleSheet.create({
    container: {
        marginHorizontal: -15 // Attach to screen border
    },
    photoPlaceholderCard: {
        marginVertical: 20,
        marginLeft: 15
    }
});
