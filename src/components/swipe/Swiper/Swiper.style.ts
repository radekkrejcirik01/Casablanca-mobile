import { StyleSheet } from 'react-native';

export const SwiperStyle = StyleSheet.create({
    container: {
        flex: 1
    },
    viewPager: {
        width: '100%',
        height: '100%'
    }
});
