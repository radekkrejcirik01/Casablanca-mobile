import { StyleSheet } from 'react-native';

export const EmailScreenStyle = StyleSheet.create({
    title: {
        marginTop: 25,
        marginHorizontal: 25
    },
    input: {
        marginTop: '20%',
        marginHorizontal: 40
    }
});
