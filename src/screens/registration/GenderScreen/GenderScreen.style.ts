import { StyleSheet } from 'react-native';

export const GenderScreenStyle = StyleSheet.create({
    title: {
        marginTop: 25,
        marginHorizontal: 25
    },
    select: {
        marginTop: '20%',
        marginHorizontal: 40
    }
});
