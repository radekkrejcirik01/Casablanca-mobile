import { StyleSheet } from 'react-native';

export const GenderScreenStyle = StyleSheet.create({
    select: {
        marginTop: '20%',
        marginHorizontal: 40,
        marginBottom: '15%'
    }
});
