import { StyleSheet } from 'react-native';

export const ShowMeScreenStyle = StyleSheet.create({
    select: {
        marginTop: '20%',
        marginHorizontal: 40,
        marginBottom: '15%'
    }
});
